FROM ghcr.io/open-webui/open-webui:v0.6.36

LABEL maintainer="Dolorestec Team" \
      version="v0.6.36" \
      description="OpenWebUI custom image for Dolorestec"

# Configurações customizadas se necessário
# COPY custom-config.yml /app/config.yml

# Expor porta
EXPOSE 8080

# Comando padrão
CMD ["bash", "start.sh"]